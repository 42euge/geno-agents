name: agents
version: "0.1.0"
description: "Agent coordination layer — registration, discovery, and presence for multi-agent systems"

venv:
  name: geno-agents
  deps:
    - "."

runtime:
  - src: geno_agents/mcp_server.py
    dst: mcp_server.py
